@keyframes settling
  0%
    box-shadow: 0 0 30px #00000012;
    opacity: 0;
    transform: scale(1.2);
  
  100%
    box-shadow: 0 0 30px #00000012;
    opacity: 1;
    transform: scale(1);


.fade-in
  animation fade-in 1s ease-out

@keyframes fade-in
  0%
    opacity 0
  100%
    opacity 1